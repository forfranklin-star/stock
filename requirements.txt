# ============================================================
# Streamlit Cloud 依赖清单
# ------------------------------------------------------------
# 重要原则：
#   1. 不要在此限制 pandas / numpy / pyarrow 的版本上限——
#      Streamlit 基础镜像已自带与其匹配的版本，强制降级会导致
#      "installer returned a non-zero exit code" 部署失败。
#   2. pyarrow 新版可能让 akshare 内部正则报 ArrowInvalid，
#      本项目主数据源为东方财富纯 urllib 直连（不经过 pyarrow），
#      akshare 仅作兜底且代码已 try/except 容错，故无需固定 pyarrow。
#   3. 只列 Streamlit 基础镜像中没有的额外包，版本只设下限。
# ============================================================

streamlit>=1.30.0
plotly>=5.18.0

# 行情/新闻/资金流：主用内置东方财富直连，以下为多源降级备用
akshare>=1.14.0
yfinance>=0.2.30
baostock>=0.8.8

# 数值计算（scikit-learn 用于模型，scipy 用于显著性检验）
scipy>=1.10.0
scikit-learn>=1.3.0
requests>=2.28.0
